接收到文件使用广播进行通知并让已经显示的Activity销毁。开启新的Activity
收到文件之后用SharedPreferences保存。
另一个类再读取SharedPreferences将已经保存的内容显示出来

每次接收的时候放在一个特定的文件夹里。然后接收完毕之后通知读取的。然后读取的时候再读取特定文件夹。
每次接收同种文件的时候把那个文件夹下的内容全部删除再接收/给文件夹加一个时间戳，用SharedPreferences保存。然后再读取